{
	"name": "cord",
	"scripts": {
		"dev": "pnpm --filter @terminallyonline/cord-playground dev",
		"build": "pnpm --filter @terminallyonline/cord build && pnpm --filter @terminallyonline/cord-playground build",
		"test": "pnpm --filter @terminallyonline/cord test",
		"test:watch": "pnpm --filter @terminallyonline/cord test:watch",
		"test:coverage": "pnpm --filter @terminallyonline/cord test:coverage"
	},
	"devDependencies": {
		"@changesets/cli": "^2.27.10",
		"typescript": "^5.3.3"
	}
}
